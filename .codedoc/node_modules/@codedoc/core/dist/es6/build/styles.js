import { join } from 'path';
import { theme } from '@connectv/jss-theme';
import { externalSheet } from '@connectv/jss-theme/external';
export function styles(config) {
    return externalSheet(theme(config.theme), '/' + config.dest.styles + '/codedoc-styles.css', join(config.dest.assets, config.dest.styles, 'codedoc-styles.css'));
}
//# sourceMappingURL=styles.js.map