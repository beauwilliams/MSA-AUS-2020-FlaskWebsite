export function isCountable(options) {
    return options.action === 'Watch'
        || options.action === 'Star'
        || options.action === 'Fork'
        || options.action === 'Issue';
}
//# sourceMappingURL=types.js.map