import { RendererLike } from '@connectv/html';
import { ThemedComponentThis } from '@connectv/jss-theme';
import { CodedocTheme } from '../../theme';
export interface FormulaOptions {
    size?: 'normal' | 'large';
    align?: 'left' | 'center';
}
export declare function Formula(this: ThemedComponentThis<CodedocTheme>, options: FormulaOptions, renderer: RendererLike<any, any>, content: any): HTMLElement;
