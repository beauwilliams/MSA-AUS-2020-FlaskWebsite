import { renderToString } from 'katex';
export function InlineFormula(_, renderer, content) {
    const marker = renderer.create("marker", null, content);
    return renderer.create("span", { _innerHTML: renderToString(marker.textContent || '') });
}
//# sourceMappingURL=inline.js.map