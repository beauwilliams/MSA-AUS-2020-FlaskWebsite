import { RendererLike } from '@connectv/html';
import { OverrideTarget, OverrideBehavior } from './types';
export interface MetaOverrideOptions {
    target?: OverrideTarget;
    behavior?: OverrideBehavior;
    property?: string;
}
export declare function MetaOverride(options: MetaOverrideOptions, renderer: RendererLike<any, any>, content: any): HTMLElement;
