import { ConfigOverride } from './override.type';
export declare type Plugin = (config: ConfigOverride) => ConfigOverride;
export declare function plug(config: ConfigOverride, plugin: Plugin): ConfigOverride;
