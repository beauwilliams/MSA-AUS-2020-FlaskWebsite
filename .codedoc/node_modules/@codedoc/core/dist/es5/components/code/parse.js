"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DefaultHighlightMark = '/*!*/';
exports.AddHighlightMark = '/*+*/';
exports.RemoveHighlightMark = '/*-*/';
function parse(code) {
    var lines = code.split('\n');
    var highlights = [];
    var linesParsed = lines.map(function (line, index) {
        if (line.startsWith(exports.DefaultHighlightMark)) {
            highlights[index] = 'highlight';
            return line.substr(exports.DefaultHighlightMark.length);
        }
        else if (line.startsWith(exports.AddHighlightMark)) {
            highlights[index] = 'added';
            return line.substr(exports.AddHighlightMark.length);
        }
        else if (line.startsWith(exports.RemoveHighlightMark)) {
            highlights[index] = 'removed';
            return line.substr(exports.RemoveHighlightMark.length);
        }
        else {
            highlights[index] = '';
            return line;
        }
    });
    var codeParsed = linesParsed.join('\n');
    return [codeParsed, linesParsed, highlights];
}
exports.parse = parse;
//# sourceMappingURL=parse.js.map