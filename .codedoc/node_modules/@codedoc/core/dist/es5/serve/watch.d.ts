import { Subject, Observable } from 'rxjs';
import { CodedocConfig } from '../config';
export declare function watch(root: string, config: CodedocConfig, notifier?: Observable<void>): Subject<string[] | "all" | "queued">;
