import { BehaviorSubject } from 'rxjs';
import { CodedocConfig } from '../config';
import { Status } from './config';
export declare function watchAssets(root: string, config: CodedocConfig, state: BehaviorSubject<{
    status: Status;
}>): import("rxjs").Observable<string>;
