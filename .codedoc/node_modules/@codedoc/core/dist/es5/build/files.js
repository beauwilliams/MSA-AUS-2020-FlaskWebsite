"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var fs_1 = require("rxline/fs");
var rxline_1 = require("rxline");
function files(list, config) {
    if (Array.isArray(list)) {
        var conf_1 = config;
        return rxline_1.line(list.map(function (path) { return ({ path: path, root: conf_1.src.base, content: undefined }); }));
    }
    else {
        var conf = list;
        return fs_1.files('.', { root: conf.src.base })
            .pick(fs_1.pathMatch(conf.src.pick))
            .drop(fs_1.pathMatch(conf.src.drop));
    }
}
exports.files = files;
//# sourceMappingURL=files.js.map