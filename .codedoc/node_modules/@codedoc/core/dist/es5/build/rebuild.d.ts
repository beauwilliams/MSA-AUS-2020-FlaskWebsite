import { Configuration } from 'webpack';
import { SimpleLine } from 'rxline';
import { File } from 'rxline/fs';
import { CodedocConfig } from '../config';
import { ContentBuilder, BuildAssets } from './types';
export declare function rebuild(files: SimpleLine<File<undefined>>, config: CodedocConfig, builder: ContentBuilder, assets: BuildAssets, webpackConfig?: Configuration): Promise<BuildAssets>;
