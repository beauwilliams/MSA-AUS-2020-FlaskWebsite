import { CompPropPlugin, PluginPriority, ComponentSignature } from '@connectv/html';
export declare class CompInputPromisePlugin<R, T> implements CompPropPlugin<R, T> {
    priority: PluginPriority;
    wireProp(name: string, prop: any, host: Node, signature: ComponentSignature): boolean;
}
