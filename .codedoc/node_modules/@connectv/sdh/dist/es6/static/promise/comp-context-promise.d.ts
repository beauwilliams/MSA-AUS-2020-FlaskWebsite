import { CompContextPlugin, PluginPriority } from '@connectv/html';
export declare class CompContextPromisePlugin<R, T> implements CompContextPlugin<R, T> {
    wireContext(_: string, value: any, recipient: any, __: any): boolean;
    priority: PluginPriority;
}
