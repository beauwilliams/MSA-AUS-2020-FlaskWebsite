import { PluginPriority, PropertyPlugin } from '@connectv/html';
export declare class PromiseClassPlugin<R, T> implements PropertyPlugin<R | Promise<RawValue>, T> {
    priority: PluginPriority;
    setprop(prop: string, target: R | Promise<RawValue>, host: HTMLElement): boolean;
}
