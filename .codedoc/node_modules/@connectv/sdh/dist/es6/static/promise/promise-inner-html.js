import { PluginPriority } from "@connectv/html";
import { attachPromise } from "../../shared/lifecycle";
export class PromiseInnerHTMLPlugin {
    constructor() {
        this.priority = PluginPriority.Fallback;
    }
    setprop(prop, target, host) {
        if (prop === '_innerHTML' && target instanceof Promise) {
            attachPromise(host, target.then(v => {
                host.innerHTML = v !== undefined ? v.toString() : '';
            }));
            return true;
        }
        return false;
    }
}
//# sourceMappingURL=promise-inner-html.js.map