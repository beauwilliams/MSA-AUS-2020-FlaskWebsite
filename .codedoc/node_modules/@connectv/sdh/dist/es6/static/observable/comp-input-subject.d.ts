import { PluginPriority, ComponentSignature, CompPropPlugin } from '@connectv/html';
export declare class CompInputSubjectPlugin<R, T> implements CompPropPlugin<R, T> {
    wireProp(name: string, prop: any, host: Node, signature: ComponentSignature): boolean;
    priority: PluginPriority;
}
