import { PinLike } from '@connectv/core';
import { PropertyPlugin, AppendPlugin, PluginPriority } from '@connectv/html';
export declare class PinPlugin<R, T> implements PropertyPlugin<R | PinLike, T>, AppendPlugin<R | PinLike, T> {
    priority: PluginPriority;
    setprop(prop: string, target: R | PinLike, host: HTMLElement): boolean;
    append(target: R | Node | PinLike, host: Node): boolean;
}
