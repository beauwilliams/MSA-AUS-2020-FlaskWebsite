import { PinLike } from '@connectv/core';
import { PropertyPlugin, PluginPriority } from "@connectv/html";
export declare class PinInnerHTMLPlugin<R, T> implements PropertyPlugin<R | PinLike, T> {
    priority: PluginPriority;
    setprop(prop: string, target: R | PinLike, host: HTMLElement): boolean;
}
