import { callTrace } from '../shared/trace';
import { createInfo } from './transport-info';
/**
 *
 * Attaches necessary transport information to given renderer factory function.
 *
 * @param factory
 * @returns the given factory function
 *
 */
export function rendererTransport(factory) {
    const trace = callTrace();
    if (!trace)
        return factory; // --> unable to get trace info, perhaps client side code
    factory.__transport_info = createInfo(factory.name, trace);
    return factory;
}
/**
 *
 * @param factory
 * @returns attached transport information of given renderer factory function, if any.
 *
 */
export function getRendererTransportInfo(factory) {
    return factory.__transport_info;
}
//# sourceMappingURL=renderer-transport.js.map