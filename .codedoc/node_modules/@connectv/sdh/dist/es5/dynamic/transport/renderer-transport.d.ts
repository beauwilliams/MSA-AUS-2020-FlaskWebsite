import { RendererLike } from '@connectv/html';
/**
 *
 * Denotes a function that returns a client-side renderer.
 *
 */
export declare type RendererFactory<R, T> = () => RendererLike<R, T>;
/**
 *
 * Attaches necessary transport information to given renderer factory function.
 *
 * @param factory
 * @returns the given factory function
 *
 */
export declare function rendererTransport<R, T>(factory: RendererFactory<R, T>): RendererFactory<R, T>;
/**
 *
 * @param factory
 * @returns attached transport information of given renderer factory function, if any.
 *
 */
export declare function getRendererTransportInfo<R, T>(factory: RendererFactory<R, T>): any;
