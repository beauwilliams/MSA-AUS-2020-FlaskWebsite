import { CompInputWithOptions, CompInputOptions } from '@connectv/html';
export interface RecipientPromise<T> extends Promise<T>, CompInputWithOptions<T> {
    recieve: (t: T | PromiseLike<T>) => void;
}
export declare function isRecipientPromise<T>(whatever: Promise<T>): whatever is RecipientPromise<T>;
export declare function recipientPromise<T>(options?: CompInputOptions<T>): RecipientPromise<T>;
