import { PropertyPlugin, AppendPlugin, PluginPriority, RawRenderer } from '@connectv/html';
export declare class PromisePlugin<R, T> implements PropertyPlugin<R | Promise<RawValue>, T>, AppendPlugin<R | Promise<RawValue | Node>, T> {
    priority: PluginPriority;
    setprop(prop: string, target: R | Promise<RawValue>, host: HTMLElement): boolean;
    append(target: R | Node | Promise<RawValue | Node>, host: Node, renderer: RawRenderer<R | Promise<RawValue | Node>, T>): boolean;
}
