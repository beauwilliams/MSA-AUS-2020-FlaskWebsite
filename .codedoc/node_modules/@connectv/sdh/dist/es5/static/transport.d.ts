import { CompPropPlugin, PluginPriority } from '@connectv/html';
export declare class TransportPlugin<R, T> implements CompPropPlugin<R, T> {
    priority: PluginPriority;
    wireProp(name: string, prop: string, host: Node): boolean;
}
