import { Observable } from 'rxjs';
import { PropertyPlugin, PluginPriority } from "@connectv/html";
export declare class ObservableInnerHTMLPlugin<R, T> implements PropertyPlugin<R | Observable<RawValue>, T> {
    priority: PluginPriority;
    setprop(prop: string, target: R | Observable<RawValue>, host: HTMLElement): boolean;
}
