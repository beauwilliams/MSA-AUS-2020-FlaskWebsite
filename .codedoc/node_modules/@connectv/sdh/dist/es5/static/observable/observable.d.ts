import { Observable } from 'rxjs';
import { PropertyPlugin, AppendPlugin, PluginPriority } from '@connectv/html';
export declare class ObservablePlugin<R, T> implements PropertyPlugin<R | Observable<RawValue>, T>, AppendPlugin<R | Observable<RawValue>, T> {
    priority: PluginPriority;
    setprop(prop: string, target: R | Observable<RawValue>, host: HTMLElement): boolean;
    append(target: R | Node | Observable<RawValue>, host: Node): boolean;
}
