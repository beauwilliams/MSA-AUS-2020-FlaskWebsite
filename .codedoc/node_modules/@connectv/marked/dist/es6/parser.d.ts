import { RendererLike } from '@connectv/html';
import { TokensList } from 'marked';
import { PartialOptions, Options } from './options';
export declare class Parser<R = unknown, T = unknown> {
    readonly options: Options<R, T>;
    private tokens;
    private token;
    private slugger;
    private inline;
    private inlineText;
    private renderer;
    constructor(options?: PartialOptions<R, T>);
    parse(tokens: TokensList, renderer: RendererLike<R, T>): HTMLElement;
    private next;
    private safeNext;
    private peek;
    private parseText;
    private tok;
}
