import { RendererLike } from '@connectv/html';
import { ComponentMap } from './comp-map';
declare type _Props = {
    [key: string]: string;
};
declare type _Marker = {
    $: HTMLElement;
    component: string;
    props: _Props;
};
export declare class InlineCompProcessor {
    readonly renderer: RendererLike<unknown, unknown>;
    readonly map: ComponentMap;
    constructor(renderer: RendererLike<unknown, unknown>, map: ComponentMap);
    process(el: HTMLElement): HTMLElement;
    markers(el: HTMLElement): _Marker[];
    props(desc: string): _Props;
}
export {};
