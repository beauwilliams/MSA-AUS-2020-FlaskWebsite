import { escape } from "../util/escape";
export function Code(props, renderer, children) {
    let code = children.join();
    if (props.lang)
        return renderer.create("pre", null,
            renderer.create("code", { class: escape(props.lang, true) }, code));
    else
        return renderer.create("pre", null,
            renderer.create("code", null, code));
}
//# sourceMappingURL=code.js.map