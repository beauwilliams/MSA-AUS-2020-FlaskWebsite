import { TokensList } from 'marked';
import { RendererLike } from '@connectv/html';
import { Options } from './options';
export interface LinkOptions {
    href: string;
    title: string;
}
export interface ImageOptions {
    src: string;
    title: string;
    alt: string;
}
export declare class InlineProcessor<R = unknown, T = unknown> {
    private renderer;
    readonly options: Options<R, T>;
    private lexer;
    constructor(tokens: TokensList, renderer: RendererLike<R, T>, options: Options<R, T>);
    process(block: string): HTMLElement;
    children(src: Node): Node[];
    buildFrom(src: Node): HTMLElement;
}
