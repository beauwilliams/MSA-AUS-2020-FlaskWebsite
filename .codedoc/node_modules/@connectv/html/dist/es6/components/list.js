import { SimpleList } from './simple-list';
import { KeyedList } from './keyed-list';
import { KeyedDeep } from '@connectv/core';
function _isKeyedListPropWithKey(props) {
    return props.key;
}
function _isKeyedListPropWithoutKey(props) {
    return props.of instanceof KeyedDeep;
}
export function List(props, renderer) {
    if (_isKeyedListPropWithKey(props))
        return renderer.create(KeyedList, { of: props.of, each: props.each, key: props.key });
    else if (_isKeyedListPropWithoutKey(props))
        return renderer.create(KeyedList, { of: props.of, each: props.each });
    else
        return renderer.create(SimpleList, { of: props.of, each: props.each });
}
//# sourceMappingURL=list.js.map