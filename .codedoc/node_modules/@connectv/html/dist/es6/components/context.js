import { set } from '../shared/context';
export function Context(props, renderer, children) {
    let scope = renderer.create("marker", null, children);
    set(scope, props);
    return scope;
}
//# sourceMappingURL=context.js.map