import { PinLike } from "@connectv/core";
import { Observable } from "rxjs";
export declare function reactiveLiteral(strings: TemplateStringsArray, ...values: (PinLike | Observable<any> | any)[]): PinLike;
export declare function rxLiteral(strings: TemplateStringsArray, ...values: Observable<any>[]): Observable<string>;
