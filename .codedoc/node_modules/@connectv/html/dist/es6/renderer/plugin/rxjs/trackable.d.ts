import { Bindable, Clearable } from "@connectv/core";
import { Observable, Subscription } from "rxjs";
export declare class Trackable<T> implements Bindable, Clearable {
    readonly obs: Observable<T>[];
    sub: Subscription;
    constructor(obs: Observable<T>[]);
    bind(): this;
    clear(): this;
}
export declare function trackable<T>(...obs: Observable<T>[]): Trackable<T>;
export default trackable;
