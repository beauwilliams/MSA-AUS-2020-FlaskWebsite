import { isPinLike, sink } from '@connectv/core';
import * as L from '../../../shared/life-cycle';
import { PluginPriority } from '../plugin';
export class PinObjectValuePlugin {
    constructor() {
        this.priority = PluginPriority.High;
    }
    setprop(prop, target, host) {
        if (prop === '_value'
            && (host instanceof HTMLOptionElement || host instanceof HTMLInputElement)
            && isPinLike(target)) {
            L.attach(target.to(sink(v => host._value = v)), host);
            return true;
        }
        else
            return false;
    }
}
//# sourceMappingURL=object-value.js.map