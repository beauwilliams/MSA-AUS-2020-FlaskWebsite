import { Subject, fromEvent } from 'rxjs';
import { PluginPriority } from '../plugin';
export class SubjectEventHandlerPlugin {
    constructor() {
        this.priority = PluginPriority.High;
    }
    setprop(prop, target, host) {
        if (prop.startsWith('on') && target instanceof Subject) {
            const event = prop.substr(2).toLowerCase();
            fromEvent(host, event).subscribe(target);
            return true;
        }
        else
            return false;
    }
}
//# sourceMappingURL=subject-event-handler.js.map