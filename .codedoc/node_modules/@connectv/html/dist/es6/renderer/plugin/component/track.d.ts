import { PluginPriority } from '../plugin';
import { CompProcessPlugin } from './basic-plugins';
import { CompType } from './types';
export declare class TrackPlugin<Renderable = RawValue, Tag = CompType<Renderable | string> | string> implements CompProcessPlugin<Renderable, Tag> {
    prepare(_: CompType<Renderable | RawValue, Tag>, __: unknown, ___: unknown, extras: {
        [name: string]: any;
    }): (node: Node) => void;
    priority: PluginPriority;
}
