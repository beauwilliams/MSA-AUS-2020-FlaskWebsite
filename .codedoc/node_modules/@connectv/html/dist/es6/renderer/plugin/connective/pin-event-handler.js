import { isPinLike, wrap } from '@connectv/core';
import { fromEvent } from 'rxjs';
import { PluginPriority } from '../plugin';
export class PinEventHandlerPlugin {
    constructor() {
        this.priority = PluginPriority.High;
    }
    setprop(prop, target, host) {
        if (prop.startsWith('on')) {
            const event = prop.substr(2).toLowerCase();
            if (isPinLike(target)) {
                wrap(fromEvent(host, event)).to(target);
                return true;
            }
            return false;
        }
        else
            return false;
    }
}
//# sourceMappingURL=pin-event-handler.js.map