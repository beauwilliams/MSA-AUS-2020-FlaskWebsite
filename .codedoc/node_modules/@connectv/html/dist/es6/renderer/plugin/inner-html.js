import { PluginPriority } from './plugin';
export class InnerHTMLPlugin {
    constructor() {
        this.priority = PluginPriority.Fallback;
    }
    setprop(prop, target, host) {
        if (prop === '_innerHTML') {
            host.innerHTML = target;
            return true;
        }
        else
            return false;
    }
}
//# sourceMappingURL=inner-html.js.map