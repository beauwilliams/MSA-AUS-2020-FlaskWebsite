import { PinLike } from '@connectv/core';
import { PluginPriority } from '../plugin';
import { PropertyPlugin } from '../basic-plugins';
export declare class PinObjectValuePlugin<R, T> implements PropertyPlugin<PinLike | R, T> {
    priority: PluginPriority;
    setprop(prop: string, target: PinLike | R | RawValue, host: HTMLElement): boolean;
}
