import { Ref } from '../ref';
import { PluginPriority } from './plugin';
export class RefPlugin {
    constructor() {
        this.priority = PluginPriority.High;
    }
    setprop(prop, target, host) {
        if (prop === '_ref' && target instanceof Ref) {
            target.resolve(host);
            return true;
        }
        else
            return false;
    }
}
//# sourceMappingURL=ref.js.map