"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("@connectv/core");
var rxjs_1 = require("rxjs");
var operators_1 = require("rxjs/operators");
function reactiveLiteral(strings) {
    var values = [];
    for (var _i = 1; _i < arguments.length; _i++) {
        values[_i - 1] = arguments[_i];
    }
    return core_1.group.apply(void 0, values.map(function (v) {
        if (core_1.isPinLike(v))
            return v;
        else if (v instanceof rxjs_1.Observable)
            return core_1.wrap(v);
        else
            return core_1.value(v);
    })).to(core_1.pack())
        .to(core_1.map(function (values) {
        return strings.reduce(function (total, piece, index) { return total + piece + (values[index] || '').toString(); }, '');
    }));
}
exports.reactiveLiteral = reactiveLiteral;
function rxLiteral(strings) {
    var values = [];
    for (var _i = 1; _i < arguments.length; _i++) {
        values[_i - 1] = arguments[_i];
    }
    return rxjs_1.combineLatest.apply(void 0, values.map(function (v) {
        if (v instanceof rxjs_1.Observable)
            return v;
        else
            return rxjs_1.of(v);
    })).pipe(operators_1.map(function (values) {
        return strings.reduce(function (total, piece, index) { return total + piece + (values[index] || '').toString(); }, '');
    }));
}
exports.rxLiteral = rxLiteral;
//# sourceMappingURL=reactive-literal.js.map