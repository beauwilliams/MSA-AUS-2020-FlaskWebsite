import { Observable } from "rxjs";
import { PinLike } from "@connectv/core";
export declare type ReactiveNameMap = {
    [name: string]: Observable<boolean> | PinLike;
};
export declare type RxNameMap = {
    [name: string]: Observable<boolean>;
};
export declare type NamesAggregatorFunc<T> = (names: string[]) => T;
export declare function toggleList(names: ReactiveNameMap): PinLike;
export declare function rxToggleList(names: RxNameMap): Observable<string>;
