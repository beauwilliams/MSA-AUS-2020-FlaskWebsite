import { RendererLike } from '../renderer/renderer-like';
import { CompType, ComponentThis } from '../renderer/plugin/component/types';
import { PinLike } from '@connectv/core';
import { Observable } from 'rxjs';
export interface ConditionalProps {
    if: PinLike | Observable<boolean>;
    then: () => Node;
    else?: () => Node;
}
export declare function Conditional(this: ComponentThis, props: ConditionalProps, renderer: RendererLike<any, any | CompType>): HTMLElement;
