import { PluginPriority, Plugin } from './plugin';
import { PostCreatePlugin } from './basic-plugins';
export declare type TagClassMap = {
    [tagName: string]: string;
};
export declare class StyledPlugin<R, T> implements PostCreatePlugin<R, T> {
    readonly map: TagClassMap;
    priority: PluginPriority;
    constructor(map: TagClassMap);
    unique(plugin: Plugin<R, T>): boolean;
    postCreate(node: Node): void;
}
export declare function styled<R, T>(map: TagClassMap): StyledPlugin<R, T>;
export default styled;
