import { Observable } from 'rxjs';
import { PluginPriority } from '../plugin';
import { PropertyPlugin, AppendPlugin } from '../basic-plugins';
export declare class ObservablePlugin<R = never, T = string> implements PropertyPlugin<R | Observable<RawValue>, T>, AppendPlugin<R | Observable<RawValue>, T> {
    priority: PluginPriority;
    setprop(prop: string, target: RawValue | R | Observable<RawValue>, host: HTMLElement): boolean;
    append(target: RawValue | R | Observable<RawValue> | Node | (RawValue | R | Observable<RawValue> | Node)[], host: Node): boolean;
}
