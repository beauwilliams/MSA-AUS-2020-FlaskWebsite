import { Subject } from "rxjs";
import { PluginPriority } from "../plugin";
import { CompInputOptions, CompType, ComponentSignature, CompInputWithOptions } from "../component/types";
import { CompPropPlugin } from "../component/basic-plugins";
export declare class CompInputSubject<T> extends Subject<T> implements CompInputWithOptions<T> {
    readonly inputOptions: CompInputOptions<T>;
    constructor(inputOptions?: CompInputOptions<T>);
}
export declare class CompInputSubjectPlugin<Renderable = RawValue, Tag = CompType<Renderable | string> | string> implements CompPropPlugin<Renderable, Tag> {
    wireProp(name: string, prop: any, node: Node, signature: ComponentSignature): boolean;
    priority: PluginPriority;
}
