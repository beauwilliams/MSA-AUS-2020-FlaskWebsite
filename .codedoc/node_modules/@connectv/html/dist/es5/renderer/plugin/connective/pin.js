"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("@connectv/core");
var L = __importStar(require("../../../shared/life-cycle"));
var plugin_1 = require("../plugin");
var PinPlugin = /** @class */ (function () {
    function PinPlugin() {
        this.priority = plugin_1.PluginPriority.Fallback;
    }
    PinPlugin.prototype.setprop = function (prop, target, host) {
        if (core_1.isPinLike(target)) {
            L.attach(target.to(core_1.sink(function (v) {
                if (typeof v === 'boolean') {
                    if (v)
                        host.setAttribute(prop, '');
                    else
                        host.removeAttribute(prop);
                }
                else
                    host.setAttribute(prop, (v !== undefined) ? v.toString() : '');
            })), host);
            return true;
        }
        else
            return false;
    };
    PinPlugin.prototype.append = function (target, host) {
        if (core_1.isPinLike(target)) {
            var node_1 = document.createTextNode('');
            L.attach(target.to(core_1.sink(function (v) { return node_1.textContent = (v !== undefined) ? v.toString() : ''; })), node_1);
            host.appendChild(node_1);
            return true;
        }
        else
            return false;
    };
    return PinPlugin;
}());
exports.PinPlugin = PinPlugin;
//# sourceMappingURL=pin.js.map