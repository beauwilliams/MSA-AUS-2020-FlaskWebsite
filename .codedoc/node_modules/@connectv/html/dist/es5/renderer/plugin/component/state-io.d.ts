import { PluginPriority } from "../plugin";
import { CompType, ComponentSignature } from "./types";
import { CompIOPlugin } from "./basic-plugins";
export declare class CompStateIOPlugin<Renderable = RawValue, Tag = CompType<Renderable | string> | string> implements CompIOPlugin<Renderable, Tag> {
    wire(_: Node, signature: ComponentSignature): void;
    priority: PluginPriority;
}
