import { Pin } from "@connectv/core";
import { PluginPriority } from "../plugin";
import { CompInputWithOptions, CompInputOptions, CompType, ComponentSignature } from "../component/types";
import { CompPropPlugin } from "../component/basic-plugins";
export declare class CompInputPin extends Pin implements CompInputWithOptions<any> {
    readonly inputOptions: CompInputOptions<any>;
    constructor(inputOptions?: CompInputOptions<any>);
}
export declare class CompInputPinPlugin<Renderable = RawValue, Tag = CompType<Renderable | string> | string> implements CompPropPlugin<Renderable, Tag> {
    wireProp(name: string, prop: any, _: Node, signature: ComponentSignature): boolean;
    priority: PluginPriority;
}
