import { Subscription } from "rxjs";
import { CompContextPlugin } from "../component/basic-plugins";
import { CompType } from "../component/types";
import { PluginPriority } from "../plugin";
export declare class CompContextPinPlugin<Renderable = RawValue, Tag = CompType<Renderable | string> | string> implements CompContextPlugin<Renderable, Tag> {
    wireContext(_: string, value: any, recipient: any, sub: Subscription): boolean;
    priority: PluginPriority;
}
