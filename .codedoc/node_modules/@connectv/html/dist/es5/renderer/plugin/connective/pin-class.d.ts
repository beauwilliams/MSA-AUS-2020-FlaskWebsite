import { PinLike } from '@connectv/core';
import { PluginPriority } from '../plugin';
import { PropertyPlugin } from '../basic-plugins';
export declare class PinClassPlugin<R = never, T = string> implements PropertyPlugin<R | PinLike, T> {
    priority: PluginPriority;
    setprop(prop: string, target: RawValue | R | PinLike, host: HTMLElement): boolean;
}
