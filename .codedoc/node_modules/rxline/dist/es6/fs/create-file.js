import { transform } from '../line/transform';
export function createFile(factory) {
    return transform(factory).combine(f => (Object.assign(Object.assign({}, f), { root: f.root || '' })));
}
//# sourceMappingURL=create-file.js.map