export function isPathFull(whatever) {
    return whatever && whatever.path && typeof whatever.path === 'string';
}
export function isFile(whatever) {
    return isPathFull(whatever) && 'root' in whatever && typeof whatever.root === 'string' &&
        'content' in whatever;
}
//# sourceMappingURL=types.js.map