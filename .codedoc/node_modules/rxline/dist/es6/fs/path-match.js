export function pathMatch(reg) {
    return function (f) {
        if (typeof f === 'string')
            return reg.test(f);
        else
            return reg.test(f.path);
    };
}
//# sourceMappingURL=path-match.js.map