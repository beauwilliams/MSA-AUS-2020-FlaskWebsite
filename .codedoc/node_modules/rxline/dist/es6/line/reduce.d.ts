import { Observable } from 'rxjs';
import { Line } from './line';
export declare type ReduceFunc<T, I> = (total: T, each: I) => T | Promise<T> | Observable<T>;
export declare function reduce<I>(func: ReduceFunc<number, I>): (l: Line<any, I>) => Promise<number>;
export declare function reduce<T, I>(func: ReduceFunc<T, I>, init: T): (l: Line<any, I>) => Promise<T>;
