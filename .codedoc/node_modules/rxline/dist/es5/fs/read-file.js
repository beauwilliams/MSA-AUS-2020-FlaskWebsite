"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var fs_1 = require("fs");
var path_1 = require("path");
function readFile(options) {
    if (options === void 0) { options = {}; }
    var _root = options.root || '';
    return function (f) {
        var path;
        var root = _root;
        if (typeof f === 'string')
            path = f;
        else {
            path = f.path;
            root = root || f.root;
        }
        var abspath = path_1.join(root, path);
        return new Promise(function (resolve, reject) {
            fs_1.readFile(abspath, function (err, res) {
                if (err)
                    reject(err);
                else
                    resolve({ path: path, root: root, content: res.toString() });
            });
        });
    };
}
exports.readFile = readFile;
//# sourceMappingURL=read-file.js.map